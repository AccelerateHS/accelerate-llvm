# Some commonly used options have been documented as comments in this file.
# For advanced use and comprehensive documentation of the format, please see:
# https://docs.haskellstack.org/en/stable/yaml_configuration/

resolver: lts-19.25

packages:
- accelerate-llvm
- accelerate-llvm-native
- accelerate-llvm-ptx

extra-deps: 
- cuda-0.11.0.0
- nvvm-0.10.0.0
- github: llvm-hs/llvm-hs
  commit: eda85a2bbe362a0b89df5adce0cb65e4e755eac5
  subdirs:
    - llvm-hs
    - llvm-hs-pure
- github: mangoiv/accelerate
  commit: c49486c618e6d999ff6864a23b4211db48b27795

  # commit: a9cf26f40cea53042659cca7d509ae2acafd3857

# Override default flag values for local packages and extra-deps
# flags: {}

# Extra per-package and global ghc options
# ghc-options:
#   llvm-hs: -optcxx=-std=c++14 -optcxx=-lstdc++ -optcxx=-fno-rtti

# Extra package databases containing global packages
# extra-package-dbs: []

# Control whether we use the GHC we find on the path
# system-ghc: true
#
# Require a specific version of stack, using version ranges
# require-stack-version: -any # Default
# require-stack-version: ">=1.9"
#
# Override the architecture used by stack, especially useful on Windows
# arch: i386
# arch: x86_64
#
# Extra directories used by stack for building
# extra-include-dirs: [/path/to/dir]
# extra-lib-dirs: [/path/to/dir]
#
# Allow a newer minor version of GHC than the snapshot specifies
# compiler-check: newer-minor
#
# vim: nospell
